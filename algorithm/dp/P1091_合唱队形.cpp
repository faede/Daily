#include <iostream>
#include <cstdio>
#include <algorithm>
#include <queue>
#include <deque>
using namespace std;
const int maxn = 100 + 10;
int a[maxn];
int n;
int dp[maxn]; // dp 以i 为中心
int main(){
	cin >> n;
	for(int i = 1; i <= n; ++i){
		scanf("%d",&a[i]);
	}
	for(int )
}